<% provide(:title, @user.username) %>

<%= render 'layouts/header' %>

<div class="row">
<aside class="col-md-4">
  <section class="user_info">
    <strong> Welcome back, <span class="name"><%= @user.firstname %></span>!</strong>
  </section>
</aside>
</div>